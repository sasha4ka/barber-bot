{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run project",
            "type": "shell",
            "command": "~/.local/bin/poetry run uvicorn book_bot.main:app --app-dir src/"
        }
    ]
}